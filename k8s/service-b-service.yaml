apiVersion: v1
kind: Service
metadata:
  name: service-b
  namespace: demo
  labels:
    app: service-b
spec:
  type: ClusterIP
  selector:
    app: service-b
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
